// API base URL for all services
export const API_URL = "http://localhost:5000/api"; // Update this to your backend URL
const IP = "192.168.0.103";
const PORT = "5000";
export const baseUrl = `http://${IP}:${PORT}/api`;
