class Group {
  constructor(id, name, members, adminId, avatar) {
    this.id = id;
    this.name = name;
    this.members = members;
    this.adminId = adminId;
    this.avatar = avatar;
  }
}

export default Group;
